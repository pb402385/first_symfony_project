when@dev:
    web_profiler:
        toolbar: true

    framework:
        profiler:
            collect: true
            collect_parameter: 'profile' # Active le profiler seulement si ?profile=1 est présent
            collect_serializer_data: true

when@test:
    framework:
        profiler:
            collect: true
            collect_serializer_data: true
