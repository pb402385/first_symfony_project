<nav class="bg-white shadow-md sticky top-0 z-50">
    <div class="max-w-7xl mx-auto px-6 py-4 flex justify-between items-center">
        <div class="flex items-center space-x-3">
            <a href="{{ url('home.index') }}">
                <i class="fa-solid fa-book-open text-3xl text-indigo-600"></i>
                <span class="text-2xl font-bold text-gray-800">DocShare</span>
            </a>
        </div>

        <div class="hidden md:flex items-center space-x-8">
            <a href="{{ url('home.index') }}" class="text-gray-700 hover:text-indigo-600 font-medium {{ app.current_route starts with 'document' ? 'a-nav-active' : ''}}">Documents</a>
            <a href="{{ url('user.index') }}" class="text-gray-700 hover:text-indigo-600 font-medium {{ app.current_route == 'user.index' ? 'a-nav-active' : ''}}">Utilisateurs</a>
        </div>

        <div class="flex items-center gap-4">
            <button onclick="window.location.href='/login'"
                    class="px-5 py-2 text-indigo-600 hover:bg-indigo-50 rounded-xl font-medium transition">
                Connexion
            </button>
            <button onclick="window.location.href='/register'"
                    class="px-6 py-2 bg-indigo-600 hover:bg-indigo-700 text-white rounded-xl font-medium transition">
                Inscription gratuite
            </button>
        </div>
    </div>
</nav>
